const rand = require("../js/random.js");

module.exports = { 
	name: "statarray",
	description: "Rolls 4d6 and drops the lowest value of the four. Does this six times, and returns the generated values.",
	execute(message,args){
			message.channel.send("STATARRAY	- TODO");
	},
};